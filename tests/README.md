Module tests are owned by the corresponding seniors. Real execution-machine tests
run in the dedicated Docker machine with /workspace/src and /workspace/tests mounted
read-only. Writable fixtures belong under the container's temporary directories.

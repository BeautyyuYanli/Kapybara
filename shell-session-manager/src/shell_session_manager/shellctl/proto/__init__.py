"""Generated protobuf packages for shellctl transports.

The canonical Python DTOs still live in
`shell_session_manager.shellctl.shared.schemas`; this package only carries the
official protobuf boundary types used by the grpclib transport.
"""
